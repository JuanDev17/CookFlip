export interface IDiscoverCard {
    title: string, 
    // type is what kind of card is it ( e.g. vegan, quick bites, midnight snacks )
    // 
    subtitle: string,
    image: string,
    type: string,
}

